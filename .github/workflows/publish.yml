name: Publish to NPM

on:
  release:
    types: [published]

jobs:
  release:
    name: Release to NPM
    runs-on: ubuntu-latest

    permissions:
      contents: read
      id-token: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: '22.x'
          cache: 'pnpm'
          registry-url: 'https://registry.npmjs.org'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build
        run: pnpm run build

      - name: Verify build output
        run: |
          test -f index.js || exit 1
          test -f index.d.ts || exit 1
          test -f dist/Gluify.js || exit 1
          test -f dist/Gluify.d.ts || exit 1
          echo "Build output verified!"

      - name: Run tests
        run: pnpm test

      - name: Publish to NPM
        run: pnpm publish --provenance --access public --no-git-checks
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

# To use this workflow:
# 1. Get your npm token: npm login, then cat ~/.npmrc
# 2. Add it to GitHub: Settings > Secrets and variables > Actions > New repository secret
#    Name: NPM_TOKEN
#    Value: Your npm token (starts with npm_...)
# 3. Create a GitHub release (not just a git tag)
# 4. The workflow will automatically publish to npm
